services:
  payment-tracker:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - ARENDA_FILE=arenda.xlsx
      - BANK_FILE=print.xlsx
      - REPORT_FILE=report.xlsx
      - GRACE_DAYS=3
